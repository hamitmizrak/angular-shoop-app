import { SearchBlogFilterPipe } from './search-blog-filter.pipe';

describe('SearchBlogFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBlogFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
